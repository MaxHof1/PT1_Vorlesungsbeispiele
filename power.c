#include <stdio.h>
#include <math.h>

int main(void)
{
    double base = 2;
    double power = 1; 
    double result;
    
    for (double i = power; i <= 16; i++)
        result = (base, i);
        printf("%ld\n", result);
    
    
    return 0;
}